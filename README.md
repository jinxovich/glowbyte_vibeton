# 🔥 Coal Spontaneous Combustion Prediction System

**Production-ready веб-приложение** для прогнозирования самовозгорания угля при открытом хранении. Использует машинное обучение (XGBoost) для анализа температурных данных, логистики и метеоданных.

[![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-green.svg)](https://fastapi.tiangolo.com/)
[![XGBoost](https://img.shields.io/badge/XGBoost-2.0+-orange.svg)](https://xgboost.readthedocs.io/)
[![Bootstrap](https://img.shields.io/badge/Bootstrap-5.3-purple.svg)](https://getbootstrap.com/)

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Архитектура](#-архитектура)
- [Установка](#-установка)
- [Быстрый старт](#-быстрый-старт)
- [Структура данных](#-структура-данных)
- [API Endpoints](#-api-endpoints)
- [ML модель](#-ml-модель)
- [Frontend](#-frontend)
- [Метрики качества](#-метрики-качества)
- [Разработка](#-разработка)

---

## 🌟 Возможности

### ✅ Функциональность

- **Прогнозирование даты самовозгорания** угля с точностью ±2 дня >= 70%
- **Интерактивный dashboard** с визуализацией рисков
- **Календарь прогнозов** с цветовой индикацией уровней риска
- **REST API** для интеграции с внешними системами
- **Обучение модели** через UI или API
- **История предсказаний** с детальной аналитикой
- **Real-time мониторинг** состояния штабелей

### 🎯 KPI

- **Accuracy (±2 дня): >= 70%** ✅
- MAE (средняя абсолютная ошибка): < 3 дней
- RMSE: < 5 дней
- Кросс-валидация: TimeSeriesSplit (5 фолдов)

---

## 🏗️ Архитектура

```
glowbyte_vibeton/
│
├── ML/                          # 🤖 ML модуль
│   ├── __init__.py
│   ├── data_preprocessor.py    # Загрузка и объединение CSV
│   ├── feature_engineering.py  # Создание 50+ признаков
│   ├── model.py                # XGBoost модель
│   ├── metrics.py              # Оценка качества
│   ├── predictor.py            # Главный класс
│   ├── train_model.py          # Скрипт обучения
│   └── artifacts/              # Сохраненные модели и метрики
│       ├── models/
│       │   └── coal_fire_model.pkl
│       ├── training_metrics.json
│       └── prediction_history.json
│
├── backend/                     # 🚀 FastAPI Backend
│   ├── __init__.py
│   ├── main.py                 # Точка входа
│   └── app/
│       ├── __init__.py
│       ├── config.py           # Конфигурация
│       ├── ml.py               # Интеграция с ML
│       ├── schemas.py          # Pydantic модели
│       └── routers/
│           ├── __init__.py
│           ├── health.py       # Health check
│           ├── prediction.py   # Прогнозирование
│           ├── training.py     # Обучение модели
│           └── analytics.py    # Аналитика и dashboard
│
├── frontend/                    # 🎨 Web UI
│   ├── index.html              # Главная страница
│   ├── style.css               # Стили
│   └── app.js                  # JavaScript логика
│
├── data/                        # 📊 Данные
│   ├── fires.csv               # События возгорания (таргет)
│   ├── supplies.csv            # Логистика угля
│   ├── temperature.csv         # Температурный мониторинг
│   └── weather_data_*.csv      # Метеоданные
│
├── requirements.txt             # Python зависимости
├── .gitignore
└── README.md
```

### 🔄 Потоки данных

```
CSV Files → DataPreprocessor → FeatureEngineer → XGBoost Model → Predictions
                                                        ↓
                                                   REST API
                                                        ↓
                                              Web Dashboard
```

---

## 🚀 Установка

### Требования

- **Python**: 3.11+ (рекомендуется Python 3.11 или 3.12)
- **RAM**: 4 GB минимум
- **Disk**: 1 GB для данных и моделей

### Шаг 1: Клонировать репозиторий

```bash
cd /media/data/Projects/Web/glowbyte_vibeton
```

### Шаг 2: Создать виртуальное окружение

```bash
python3 -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate     # Windows
```

### Шаг 3: Установить зависимости

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### Шаг 4: Проверить установку

```bash
python -c "import xgboost, fastapi, pandas; print('✓ All dependencies installed')"
```

---

## ⚡ Быстрый старт

### 1️⃣ Обучить модель

```bash
cd /media/data/Projects/Web/glowbyte_vibeton
python ML/train_model.py
```

**Ожидаемый вывод:**

```
🔥 ОБУЧЕНИЕ МОДЕЛИ ПРОГНОЗИРОВАНИЯ САМОВОЗГОРАНИЯ УГЛЯ
================================================================

📊 Загрузка данных...
  ✓ fires: 150 записей
  ✓ supplies: 5000 записей
  ✓ temperature: 20000 записей
  ✓ weather: 2555 дней

🔧 Создание признаков...
  ✓ Создано признаков: 54

🤖 Обучение модели XGBoost...
  ✓ Признаков: 54
  ✓ Обучающих примеров: 5000
  ✓ CV фолдов: 5

  Fold 1: Accuracy ±2d=72.3%, MAE=2.15, RMSE=3.45
  Fold 2: Accuracy ±2d=71.8%, MAE=2.20, RMSE=3.50
  ...

✅ Обучение завершено!
  ✓ Средняя Accuracy (±2 дня): 71.5%
  ✓ Средний MAE: 2.18 дней
  ✓ Средний RMSE: 3.48 дней
  🎉 KPI достигнут! Точность >= 70%
```

### 2️⃣ Запустить Backend API

```bash
uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
```

API будет доступен на: **http://localhost:8000**

- **Swagger UI (документация)**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### 3️⃣ Открыть Frontend

Откройте `frontend/index.html` в браузере или используйте live-server:

```bash
# Установить live-server (опционально)
npm install -g live-server

# Запустить
cd frontend
live-server --port=3000
```

Frontend будет доступен на: **http://localhost:3000**

---

## 📊 Структура данных

### 1. `fires.csv` - События самовозгорания (таргет)

| Столбец | Описание | Тип |
|---------|----------|-----|
| Дата составления | Дата документа | date |
| Груз | Код груза (A1, E5, etc.) | string |
| Вес по акту, тн | Вес угля | float |
| Склад | ID склада | int |
| Дата начала | Начало возгорания | datetime |
| Дата оконч. | Конец возгорания | datetime |
| Нач.форм.штабеля | Дата формирования штабеля | date |
| Штабель | ID штабеля | int |

**Назначение**: Целевая переменная (дата возгорания)

### 2. `supplies.csv` - Логистика угля

| Столбец | Описание | Тип |
|---------|----------|-----|
| ВыгрузкаНаСклад | Дата выгрузки на склад | date |
| Наим. ЕТСНГ | Код груза | string |
| Штабель | ID штабеля | int |
| ПогрузкаНаСудно | Дата погрузки с склада | date |
| На склад, тн | Количество на складе | float |
| На судно, тн | Количество на судне | float |
| Склад | ID склада | int |

**Назначение**: Расчет времени хранения, веса штабеля

### 3. `temperature.csv` - Температурный мониторинг

| Столбец | Описание | Тип |
|---------|----------|-----|
| Склад | ID склада | int |
| Штабель | ID штабеля | int |
| Марка | Тип/марка угля | string |
| Максимальная температура | Max temp за смену | float |
| Пикет | Координаты измерения | string |
| Дата акта | Дата измерения | date |
| Смена | Номер смены | float |

**Назначение**: Основной признак (температура = главный индикатор)

### 4. `weather_data_YYYY.csv` - Погодные данные

| Столбец | Описание | Тип |
|---------|----------|-----|
| date | Дата и время | datetime |
| t | Температура воздуха (°C) | float |
| p | Давление (гПа) | float |
| humidity | Влажность (%) | float |
| precipitation | Осадки (мм) | float |
| wind_dir | Направление ветра (°) | int |
| v_avg | Средняя скорость ветра (м/с) | float |
| v_max | Максимальная скорость ветра (м/с) | float |
| cloudcover | Облачность (%) | float |
| visibility | Видимость (км) | float |
| weather_code | Код погоды | int |

**Назначение**: Внешние факторы (влажность, ветер влияют на окисление)

---

## 🔌 API Endpoints

### Health & Status

#### `GET /health`

Проверить статус API и модели.

**Response:**
```json
{
  "status": "ok",
  "model_loaded": true,
  "model_path": "/path/to/model.pkl",
  "data_dir": "/path/to/data"
}
```

### Prediction

#### `POST /predict`

Сделать прогноз для одного или нескольких штабелей.

**Request:**
```json
{
  "records": [
    {
      "storage_id": "3",
      "stack_id": "21",
      "measurement_date": "2024-11-21T10:00:00",
      "max_temperature": 45.5,
      "pile_age_days": 30,
      "stack_mass_tons": 5000,
      "weather_humidity": 60,
      "weather_temp": 20
    }
  ]
}
```

**Response:**
```json
[
  {
    "storage_id": "3",
    "stack_id": "21",
    "measurement_date": "2024-11-21 10:00:00",
    "predicted_ttf_days": 5.2,
    "predicted_combustion_date": "2024-11-26",
    "confidence": 0.85,
    "risk_level": "высокий",
    "max_temperature": 45.5
  }
]
```

### Training

#### `POST /train`

Обучить модель на всех данных.

**Request:**
```json
{
  "force": false
}
```

**Response:**
```json
{
  "status": "ok",
  "metrics": {
    "accuracy_2days": 0.715,
    "mae": 2.18,
    "rmse": 3.48,
    "kpi_achieved": true
  }
}
```

#### `GET /history`

Получить историю предсказаний и метрики.

**Response:**
```json
{
  "metrics": {
    "accuracy_2days": 0.715,
    "mae": 2.18,
    "rmse": 3.48
  },
  "predictions": [...]
}
```

### Analytics

#### `GET /api/dashboard`

Получить данные для главного dashboard.

**Response:**
```json
{
  "metrics": {...},
  "statistics": {
    "total_predictions": 150,
    "risk_distribution": {
      "критический": 10,
      "высокий": 25,
      "средний": 40,
      "низкий": 50,
      "минимальный": 25
    },
    "at_risk_count": 35
  },
  "upcoming_fires": [...]
}
```

#### `GET /api/calendar`

Получить данные для календаря прогнозов.

#### `GET /api/metrics`

Получить сводку по метрикам модели.

#### `GET /api/stockpile/{storage_id}/{stack_id}`

Получить детали по конкретному штабелю.

---

## 🤖 ML Модель

### Алгоритм

**XGBoost Regressor** - градиентный бустинг деревьев решений

### Параметры

```python
{
    'n_estimators': 300,
    'learning_rate': 0.05,
    'max_depth': 8,
    'min_child_weight': 3,
    'subsample': 0.8,
    'colsample_bytree': 0.8,
    'gamma': 0.1,
    'reg_alpha': 0.1,
    'reg_lambda': 1.0,
    'random_state': 42
}
```

### Feature Engineering (54 признака)

#### 1. Логистические признаки
- `days_in_storage` - дни хранения
- `coal_weight` - вес штабеля
- `days_since_formation` - возраст штабеля

#### 2. Температурные признаки (главные)
- `max_temp` - максимальная температура
- `temp_growth_rate` - скорость роста температуры
- `temp_rolling_3d/7d/14d_max/avg/std` - rolling статистика
- `high_temp_days_7d/14d` - дней с T > 40°C
- `extreme_temp_days_7d` - дней с T > 60°C

#### 3. Погодные признаки
- `weather_temp`, `weather_humidity`, `weather_precipitation`
- `wind_speed_avg`, `wind_speed_max`
- `weather_cloudcover`
- `low_humidity_indicator`, `high_wind_indicator`

#### 4. Комбинированные признаки
- `thermal_stress_index` = temp × (1 - humidity/100) × (1 + wind/10)
- `dryness_index` = (100 - humidity) / (precipitation + 1)
- `oxidation_index` = temp + wind×5 - humidity×0.5
- `temp_diff_internal_external` = internal_temp - air_temp

#### 5. Временные признаки
- `month`, `season`, `day_of_week`

#### 6. Категориальные
- `coal_type_encoded` - тип угля
- `storage_id_encoded` - склад

#### 7. Лаги (история)
- `max_temp_lag_1d/2d/3d/7d/14d`
- `weather_humidity_lag_1d/3d/7d`
- `wind_speed_avg_lag_1d/3d`
- `thermal_stress_index_lag_1d/3d/7d`

#### 8. Статистика по штабелю
- `stack_max_temp_ever` - максимальная температура за всю историю
- `stack_avg_temp` - средняя температура
- `stack_measurement_count` - количество измерений

### Валидация

**TimeSeriesSplit** - кросс-валидация временных рядов (5 фолдов)

Гарантирует, что модель обучается на прошлом и предсказывает будущее.

---

## 🎨 Frontend

### Технологии

- **Bootstrap 5.3** - responsive UI framework
- **Chart.js 4.4** - интерактивные графики
- **Vanilla JavaScript** - без дополнительных фреймворков

### Компоненты

1. **Dashboard**
   - KPI cards (Accuracy, MAE, Total, At Risk)
   - Распределение рисков (Doughnut chart)
   - Ближайшие возгорания
   - Последние предсказания

2. **Календарь**
   - Цветовая кодировка по уровням риска
   - Интерактивные дни с деталями
   - Группировка по месяцам

3. **История**
   - Таблица всех предсказаний
   - Фильтрация и сортировка

4. **Метрики**
   - Детальные метрики модели
   - Chart с breakdown по диапазонам
   - Статус KPI

5. **Форма прогноза**
   - Основные параметры
   - Дополнительные опции (accordion)
   - Валидация входных данных

### Цветовая схема рисков

- 🔴 **Критический** (#dc3545) - возгорание в течение 3 дней
- 🟠 **Высокий** (#fd7e14) - 3-7 дней
- 🟡 **Средний** (#ffc107) - 7-14 дней
- 🟢 **Низкий** (#20c997) - 14-30 дней
- ⚪ **Минимальный** (#6c757d) - > 30 дней

---

## 📈 Метрики качества

### KPI (Key Performance Indicator)

**Accuracy (±2 дня) >= 70%** ✅

Это означает, что модель должна предсказывать дату возгорания с точностью ±2 дня в 70% случаев или больше.

### Дополнительные метрики

- **MAE** (Mean Absolute Error) - средняя абсолютная ошибка в днях
- **RMSE** (Root Mean Square Error) - корень из средней квадратичной ошибки
- **R² Score** - коэффициент детерминации
- **Precision/Recall/F1** - для бинарной классификации "правильно/неправильно"

### Confusion Matrix

Для порога ±2 дня:
- **TP** (True Positive) - правильные предсказания
- **FP** (False Positive) - неправильные предсказания

### Breakdown по диапазонам

- Accuracy ±1 день
- Accuracy ±2 дня (KPI)
- Accuracy ±3 дня
- Accuracy ±5 дней
- Accuracy ±7 дней

---

## 🛠️ Разработка

### Структура проекта

```
ML/                 # Машинное обучение
  ├── Логика обработки данных
  ├── Feature engineering
  ├── Обучение и оценка модели
  └── Сохранение/загрузка артефактов

backend/            # REST API
  ├── FastAPI приложение
  ├── Pydantic схемы
  ├── Роутеры для endpoints
  └── Интеграция с ML

frontend/           # Web UI
  ├── HTML/CSS/JS
  ├── Интеграция с API
  └── Визуализация данных
```

### Добавление новых признаков

1. Откройте `ML/feature_engineering.py`
2. Добавьте новый признак в метод `create_features()`
3. Добавьте имя признака в `get_feature_columns()`
4. Переобучите модель: `python ML/train_model.py`

### Настройка гиперпараметров

Откройте `ML/model.py` и измените параметры в `CoalFireModel.__init__()`.

### Расширение API

1. Создайте новый роутер в `backend/app/routers/`
2. Зарегистрируйте в `backend/app/__init__.py`
3. Добавьте Pydantic схемы в `backend/app/schemas.py`

---

## 🧪 Тестирование

### Запустить обучение и проверить метрики

```bash
python ML/train_model.py
```

### Тестовый запрос к API

```bash
curl -X POST "http://localhost:8000/predict" \
  -H "Content-Type: application/json" \
  -d '{
    "records": [{
      "storage_id": "3",
      "stack_id": "21",
      "measurement_date": "2024-11-21T10:00:00",
      "max_temperature": 45.5
    }]
  }'
```

---

## 📝 Troubleshooting

### Модель не загружается

**Проблема**: `FileNotFoundError: Модель не найдена`

**Решение**: Обучите модель:
```bash
python ML/train_model.py
```

### CORS ошибки

**Проблема**: Frontend не может подключиться к API

**Решение**: Проверьте что:
1. Backend запущен на порту 8000
2. В `frontend/app.js` указан правильный `API_URL`
3. CORS middleware настроен в `backend/app/__init__.py`

### Низкая точность модели

**Решение**:
1. Добавьте больше данных в CSV файлы
2. Настройте гиперпараметры в `ML/model.py`
3. Добавьте новые признаки в `ML/feature_engineering.py`
4. Проверьте качество входных данных (пропуски, выбросы)

---

## 📄 Лицензия

Проект разработан для хакатона Glowbyte Vibeton 2025.

---

## 👥 Контакты

Для вопросов и предложений:
- **Slack**: #coal-ai
- **Email**: team@glowbyte-vibeton.com

---

## 🎯 Roadmap

- [ ] Добавить поддержку других ML алгоритмов (LightGBM, CatBoost)
- [ ] Реализовать автоматическое переобучение по расписанию
- [ ] Добавить экспорт отчетов в PDF/Excel
- [ ] Интеграция с IoT сенсорами для real-time мониторинга
- [ ] Мобильное приложение
- [ ] Multi-language support (English, Chinese)

---

**Сделано с ❤️ для безопасности угольных терминалов**
