# ✅ ДАШБОРД ГОТОВ!

**Дата:** 22 ноября 2025  
**Статус:** ✅ ВСЕ ЗАМЕРЫ СОХРАНЯЮТСЯ И ОТОБРАЖАЮТСЯ

---

## 🎯 ЧТО СДЕЛАНО

### 1. Backend API ✅
- ✅ Обновлен `backend/app/routers/prediction.py`
  - Интеграция с `SimpleCoalFirePredictor`
  - Сохранение всех предсказаний в БД
  - Endpoint `/predict/dashboard` для получения всех данных

### 2. Database ✅
- ✅ Все замеры сохраняются в таблицу `predictions`:
  - `storage_id`, `stack_id` - идентификаторы
  - `predicted_days` - прогноз (дни до пожара)
  - `confidence` - уверенность модели (%)
  - `risk_level` - уровень риска
  - `input_data` - входные данные (JSON)
  - `created_at` - дата и время замера

### 3. Frontend Dashboard ✅
- ✅ Обновлен `frontend/src/pages/Dashboard.tsx`
  - **4 KPI карточки**:
    - Всего замеров
    - Критических (< 7 дней)
    - Безопасных
    - Средняя уверенность
  - **2 графика**:
    - Динамика прогнозов (линейный)
    - Уверенность модели (столбчатый)
  - **Таблица всех замеров**:
    - ID, Склад, Штабель
    - Прогноз (дни)
    - Риск (с цветовой индикацией)
    - Уверенность (%)
    - Дата и время

---

## 🚀 КАК ЗАПУСТИТЬ

### 1. Backend
```bash
# В одном терминале
cd /media/data/Projects/Web/glowbyte_vibeton

# Активировать виртуальное окружение (если есть)
source .venv/bin/activate  # Linux/Mac
# .venv\Scripts\activate  # Windows

# Запустить backend
uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

Backend будет доступен на: http://localhost:8000

### 2. Frontend
```bash
# В другом терминале
cd /media/data/Projects/Web/glowbyte_vibeton/frontend

# Установить зависимости (если нужно)
npm install

# Запустить frontend
npm run dev
```

Frontend будет доступен на: http://localhost:5173 (или другой порт, указанный Vite)

---

## 📊 КАК ИСПОЛЬЗОВАТЬ

### Шаг 1: Регистрация/Логин
1. Откройте frontend: http://localhost:5173
2. Зарегистрируйтесь или войдите

### Шаг 2: Сделайте прогноз
1. Перейдите в раздел "Прогноз" (Predict)
2. Введите данные:
   - Склад ID (storage_id)
   - Штабель ID (stack_id)
   - Температура (°C)
   - Возраст штабеля (дней)
   - Масса (тонн)
   - Влажность (%)
   - Температура воздуха (°C)
   - Скорость ветра (м/с)
3. Нажмите "Получить прогноз"

### Шаг 3: Смотрите дашборд
1. Перейдите в раздел "Дашборд" (Dashboard)
2. Увидите:
   - **Статистику**: сколько замеров, сколько критических
   - **Графики**: динамика прогнозов и уверенности
   - **Таблицу**: все замеры с подробностями

### Шаг 4: Обновление
- Нажмите кнопку "🕐 Обновить" в правом верхнем углу
- Или просто обновите страницу

---

## 🎨 ВНЕШНИЙ ВИД

### Дашборд включает:

```
┌─────────────────────────────────────────────────────────────┐
│  📊 Дашборд прогнозов                    [🕐 Обновить]     │
│  Мониторинг всех замеров и предсказаний                    │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │  Всего   │ │Критичес- │ │Безопас-  │ │  Сред.   │      │
│  │ замеров  │ │   ких    │ │   ных    │ │уверен-   │      │
│  │    42    │ │     8    │ │    34    │ │  ность   │      │
│  └──────────┘ └──────────┘ └──────────┘ │   75%    │      │
│                                          └──────────┘      │
├─────────────────────────────────────────────────────────────┤
│  ┌────────────────────┐  ┌────────────────────┐            │
│  │ 📈 Динамика        │  │ 🎯 Уверенность     │            │
│  │    прогнозов       │  │    модели          │            │
│  │  [График линейный] │  │  [График столбчат] │            │
│  └────────────────────┘  └────────────────────┘            │
├─────────────────────────────────────────────────────────────┤
│  📋 Все замеры (42)                                         │
│  ┌─────┬──────┬────────┬────────┬──────┬────────┬────────┐ │
│  │ ID  │Склад │Штабель │Прогноз │ Риск │Уверен-│  Дата  │ │
│  ├─────┼──────┼────────┼────────┼──────┼────────┼────────┤ │
│  │ #42 │  11  │   11   │ 15 дн. │🟢низ.│  85%   │22.11..│ │
│  │ #41 │  11  │   21   │  7 дн. │🟠выс.│  75%   │22.11..│ │
│  │ #40 │  3   │   13   │ 21 дн. │🟢низ.│  60%   │22.11..│ │
│  └─────┴──────┴────────┴────────┴──────┴────────┴────────┘ │
└─────────────────────────────────────────────────────────────┘
```

---

## 📡 API ENDPOINTS

### 1. Сделать прогноз
```bash
POST http://localhost:8000/predict/
Authorization: Bearer <token>
Content-Type: application/json

{
  "storage_id": "11",
  "stack_id": "11",
  "max_temperature": 50,
  "pile_age_days": 40,
  "stack_mass_tons": 5000,
  "weather_humidity": 50,
  "weather_temp": 15,
  "wind_speed": 3
}
```

**Ответ:**
```json
{
  "id": 42,
  "storage_id": "11",
  "stack_id": "11",
  "predicted_ttf_days": 10.3,
  "risk_level": "средний",
  "confidence": 0.75,
  "created_at": "2025-11-22T12:30:00"
}
```

### 2. Получить дашборд
```bash
GET http://localhost:8000/predict/dashboard
Authorization: Bearer <token>
```

**Ответ:**
```json
{
  "total_predictions": 42,
  "critical_count": 8,
  "avg_confidence": 75,
  "risk_distribution": {
    "критический": 3,
    "высокий": 5,
    "средний": 10,
    "низкий": 20,
    "минимальный": 4
  },
  "all_predictions": [
    {
      "id": 42,
      "storage_id": "11",
      "stack_id": "11",
      "predicted_days": 15,
      "risk_level": "низкий",
      "confidence": 85,
      "created_at": "2025-11-22T12:30:00",
      "input_data": {...}
    },
    ...
  ]
}
```

### 3. История (старый endpoint, тоже работает)
```bash
GET http://localhost:8000/predict/history?limit=100
Authorization: Bearer <token>
```

---

## 🎨 ЦВЕТОВАЯ ИНДИКАЦИЯ РИСКОВ

| Риск | Цвет | Дни до пожара | Бейдж |
|------|------|---------------|-------|
| **Критический** | 🔴 Красный | < 3 дней | `bg-red-100` |
| **Высокий** | 🟠 Оранжевый | 3-7 дней | `bg-orange-100` |
| **Средний** | 🟡 Желтый | 7-14 дней | `bg-yellow-100` |
| **Низкий** | 🟢 Зеленый | 14-30 дней | `bg-green-100` |
| **Минимальный** | ⚪ Серый | > 30 дней | `bg-gray-100` |

---

## 🔧 TROUBLESHOOTING

### Backend не запускается
```bash
# Проверить что модель обучена
python ML/train_simple.py

# Проверить что БД существует
ls backend/sql_app.db

# Создать БД если нужно
python -c "from backend.app.database import Base, engine; Base.metadata.create_all(bind=engine)"
```

### Frontend не подключается к Backend
1. Проверьте что backend запущен: http://localhost:8000/docs
2. Проверьте `API_URL` в `frontend/src/lib/api.ts`
3. Проверьте CORS в backend

### Нет данных в дашборде
1. Сначала сделайте хотя бы один прогноз
2. Проверьте что вы залогинены
3. Нажмите кнопку "Обновить"

---

## ✅ ГОТОВО К ИСПОЛЬЗОВАНИЮ!

**Все замеры автоматически:**
- ✅ Сохраняются в базу данных
- ✅ Отображаются в дашборде
- ✅ Доступны через API
- ✅ Показываются в таблице
- ✅ Отображаются на графиках

**Можно делать сколько угодно прогнозов - все будут сохранены и видны в дашборде!** 🎉

---

**Версия:** 1.0 (PRODUCTION-READY)  
**Дата:** 22 ноября 2025

